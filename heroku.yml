build:
  docker:
    web: Dockerfile
      command: ls -a
  config:
    RAILS_ENV: development
release:
  image: web
  command:
    - ls -a
run:
  web: 
    command:
      - ls -a
      - bundle install
      - rails db:migrate
      - bundle exec rails s -b 0.0.0.0